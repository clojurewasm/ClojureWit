/.cpcache/
/target/
/out/
/.tools/
/.cljwit/
*.wasm
!bench/**/*.wasm
/bench/s0/out/
.DS_Store
.nrepl-port
/result
/.ref/

# cargo build output for dev/spike/rust_call_cost (Cargo.lock is committed;
# it pins the wasmtime crate to the version in tools.json)
dev/spike/rust_call_cost/target/

# A JVM that faults inside wasmtime drops one of these in the repository root.
# That has happened twice: a null export index passed to get_func, and a
# recursive walk sharing its out-parameters.
hs_err_pid*.log

# npm (dev-loop assembler, doc/design/0026)
node_modules/
